---
import LenisFeelDemo from "@/components/lab/LenisFeelDemo.astro";

let a = 1
const fun = ()=>{
    
}

let sleep = (delay)=>{
    return new Promise((resolve)=>{
        setTimeout(resolve, delay)
    })
}
---

<LenisFeelDemo variant="steady" />
