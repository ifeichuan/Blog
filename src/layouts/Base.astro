---
import BaseHead from "../components/BaseHead.astro";
import { SITE_DESCRIPTION, SITE_TITLE } from "../consts";
import path from "node:path";
import Mouse from "../components/Mouse.astro";
import Nav from "@/components/Nav.astro";
import type { Image } from "astro:assets";

import Noise from "@/components/Noise";
import mesh from "@/assets/mesh.png";
export type Props = {
  title?: string;
  description?: string;
  banner?: string;
};
const {
  title = SITE_TITLE,
  description = SITE_DESCRIPTION,
  banner = "https://s2.loli.net/2025/10/29/EucqwWi18kdsRBp.png",
}: Props = Astro.props;
---

<!doctype html>
<html>
  <head>
    <BaseHead title={title} description={description} />
  </head>
  <body>
    <Noise client:visible />
    <Mouse />
    <Nav />
    <main
      class=`background w-screen  h-full  bg-fixed bg-no-repeat bg-cover `
      style={`background-image: url('${banner}');`}
    >
      <slot />
    </main>
    <!-- <Footer /> -->
    <script></script>
  </body>
</html>
